# Vendored front-end assets are stored and checked out byte-for-byte as they
# were supplied with the technical assignment. Without this, a checkout on
# Windows rewrites their line endings, so the bytes served would no longer match
# the files the customer approved.
static/** -text
