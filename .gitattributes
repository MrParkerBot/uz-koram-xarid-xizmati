# Vendored front-end assets are stored and checked out byte-for-byte as they
# were supplied with the technical assignment, so a checkout on Windows does
# not rewrite their line endings.
xarid/static/xarid/css/** -text
xarid/static/xarid/js/bootstrap.bundle.min.js -text
