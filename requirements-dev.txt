# Development and gate dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
# Pinned so the pull request gate enforces the same rules on every machine.
ruff==0.16.7

# Reading a QR code back out of a stamped PDF, so the test for
# TASK-UZK-032 decodes the stamp rather than trusting it - a test
# that only checked the file had changed would pass on a blank
# square. Development only: the application writes QR codes and
# never scans one.
opencv-python-headless==5.0.0
