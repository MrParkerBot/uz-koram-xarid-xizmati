# Runtime dependencies. Pinned so that an empty environment installs exactly
# what was validated.
Django==5.1.1

# The template engine every application page renders through.
Jinja2==3.1.6

# The approval QR code (DEC-027): qrcode draws it, Pillow writes it as a
# one-page PDF, pypdf merges that page onto the attachment.
qrcode==8.2
Pillow==10.4.0
pypdf==6.6.0

# Yuklab olish (REQ-ARIZA-002): openpyxl writes the Excel workbook and
# reportlab lays the same table out as a PDF.
openpyxl==3.1.5
reportlab==5.0.1
